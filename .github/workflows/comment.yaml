name: "Ping someone in the comment"

on:
  pull_request:
    types:
      - opened
      - reopened

jobs:
  main:
    name: Ping someone in the comment section of PR
    runs-on: ubuntu-latest
    steps:
      - name: Pulls AWS Credentials & Logs in.
        env:
          AWS_ACCESS_KEY_ID: 1212isadfaskjbxzasdkfsadfaslkdfj
          AWS_SECRET_ACCESS_KEY: AS2IJO1BKALKLJ
          AWS_SESSION_TOKEN: ejsakldjfoiewjfasoifjaweoifjewfaoweijfwaoiefjaweoif.sadfjaowiefjawoiefjweaofiajeoifjawef.asdfisjadfoidsafj
        run: echo "hi"
      - uses: schoi-godaddy/ping-reviewer-aksion@v1.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          who-to-ping: "schoi-godaddy"
